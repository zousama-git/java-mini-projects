# Password Strength Checker (Java Console Application)

## Overview
This program evaluates the strength of a user-entered password based on length, uppercase letters, numbers, and special characters.

## Concepts Covered
* User input handling with `Scanner`
* String pattern matching using regular expressions (`matches`)
* Conditional logic (`if-else`) for validation
* Boolean variables to track criteria
* Calculating and categorizing password strength